<div id="fb-root"></div>
<script src='/javascripts/jquery.cookie.js'></script>
<script>
  var setLocationToHome = function(response) {
    window.location = '/';
  };

  window.fbAsyncInit = function() {
    FB.init({
      appId: "<%= FacebookConfig.app_id %>",
      status: true,
      cookie: true,
      xfbml: true
    });

    FB.Event.subscribe('auth.login', setLocationToHome);
    FB.Event.subscribe('auth.logout', setLocationToHome);
  };

  (function() {
    var e = document.createElement("script");
    e.async = true;
    e.src = window.location.protocol + "//connect.facebook.net/en_US/all.js";
    document.getElementById("fb-root").appendChild(e);
  }());
</script>
